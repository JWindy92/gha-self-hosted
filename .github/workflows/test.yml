name: Simple Test Workflow

on:
  push:
    branches:
      - on-push
  workflow_dispatch:


jobs:
  build:
    # runs-on: [self-hosted, linux, x64]
    runs-on: [8b486727de86]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Run a simple script
      run: echo "Hello, GitHub Actions!"