name: Simple Test Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: [0b118465d749]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Run a simple script
      run: echo "Hello, GitHub Actions!"